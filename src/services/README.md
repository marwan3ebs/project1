# Services

Use this folder for side effects and external integrations.

Recommended services:

- `storageService` for AsyncStorage.
- `apiClient` for backend calls.
- `agreementService` for agreement code, expiry, and renewal rules.
- `reportService` for biweekly reports and exports.
- `notificationService` for local and push notifications.
