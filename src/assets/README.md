# Assets

Expo app icons and splash assets currently remain in the root `assets/` folder because `app.json` references those paths directly.

Use this folder later for feature-level images or illustrations that are imported by source code.
